services:
  - type: web
    name: ai4sustain
    runtime: node
    buildCommand: npm install
    startCommand: node server/server.js
    plan: free
    envVars:
      - key: OPENAI_API_KEY
        sync: false
      - key: PORT
        value: 3000
